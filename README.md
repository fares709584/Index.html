# Usemee Chatbot

This project is a self-contained chat app that works directly from the browser.

## Run locally

1. Open the `index.html` file in your browser.
2. No server is required for basic chat functionality.

## Notes

- The app now works without `node server.js`.
- Image analysis is not available in the offline/local version, but text chat works directly from `index.html`.
